# Database configuration
spring.datasource.url=jdbc:postgresql://postgres-oauth2:5432/oauth2_db
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# OAuth2 configuration for GitHub
spring.security.oauth2.client.registration.github.client-id=Ov23lie8L9rimJFONVyX
spring.security.oauth2.client.registration.github.client-secret=bee70330aa7d8f14c305cb112f1edeababbc2f7f
spring.security.oauth2.client.registration.github.redirect-uri=http://localhost:8080/login/oauth2/code/github  # Убедитесь, что этот URL верен

server.servlet.session.cookie.name=JSESSIONID
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=lax

# Eureka configuration
eureka.client.service-url.defaultZone=http://discovery:8700/eureka

# Server configuration
server.port=8080

# Session configuration
spring.session.timeout=30m
server.servlet.session.timeout=30m

# Logging для отладки
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG


spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
